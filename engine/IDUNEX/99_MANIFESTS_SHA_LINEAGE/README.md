# IDUNEX 99_MANIFESTS_SHA_LINEAGE

Canonical current-tree manifest and lineage layer for IDUNEX v1.0.0.

The six internal manifest carriers are non-self-referential. Their coverage is
completed externally by `governance/baseline/IDUNEX_CURRENT_TREE_MANIFEST.json`,
which hashes every physical file under `engine/IDUNEX/`, including the carriers.

Current correction mode: `DIRECT_CANONICAL_NO_PATCH`.
Semantic version: `v1.0.0`.
Motor status: `EN_REVISION`.
M02 result: `M02_FAIL`.
Creative output certified: `FALSE`.
Release authorized: `FALSE`.

`IDUNEX_CURRENT_TREE_SHA256.txt` is a deterministic repository-tree aggregate,
not a ZIP SHA, release companion, certificate or authorization to generate Demo.
