# Projects install here

This directory is intentionally empty in the motor baseline.

Rules:
- The motor contains no concrete projects.
- The motor contains no concrete subject/model names.
- Generated projects are installed here only after a separate project package passes its own audit.
- Until then this folder remains a neutral mount point.
