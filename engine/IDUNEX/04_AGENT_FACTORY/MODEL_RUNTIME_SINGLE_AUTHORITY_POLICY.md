# MODEL_RUNTIME_SINGLE_AUTHORITY_POLICY

One model equals one active model authority per agent. JSON/MD/DOCX variants may coexist only when exactly one is active_runtime=true and the rest are non-runtime references.
